public class Audi extends Car{

    public Audi(int yearOfRelease, String transmissionType, String model, int energyCapacity, int maxSpeed, String color) {
        super(yearOfRelease, transmissionType, model, energyCapacity, maxSpeed, color);
    }
}
