public class Toyota extends Car{

    public Toyota(int yearOfRelease, String transmissionType, String model, int energyCapacity, int maxSpeed, String color) {
        super(yearOfRelease, transmissionType, model, energyCapacity, maxSpeed, color);
    }
}
